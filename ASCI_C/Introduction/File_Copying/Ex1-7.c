#include <stdio.h>

/**
 * main - Entry Point
 * @void no parameter
 *
 * Description: Write a program to print the value of EOF
 */

int main(void)
{
	printf("EOF = %d\n", EOF);
	return (0);	//SUCCESS. Exit execution
}
